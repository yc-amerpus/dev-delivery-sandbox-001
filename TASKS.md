# Tasks

- [x] Fix the typo in the top heading of `README.md`: change `# Dec-Delivery Sandbox` to `# Dev-Delivery Sandbox`. Commit the change with a Conventional Commit message and `Refs: {{delivery_id}}` trailer.
- [x] Open a PR back to `main` with the standard body template (Summary, Linked spec, Changes, Tests, Notes for review, `Refs: {{delivery_id}}`). PR: https://github.com/yc-amerpus/dev-delivery-sandbox-001/pull/1
