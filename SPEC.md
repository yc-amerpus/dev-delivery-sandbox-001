# Spec

Fix the typo in the top heading of `README.md` so it reads `# Dev-Delivery Sandbox`. Open a PR back to `main`.

This repo has exactly one task: correct the heading and open a PR. No other changes are in scope. The PR should follow the standard body template described in `TASKS.md`.
